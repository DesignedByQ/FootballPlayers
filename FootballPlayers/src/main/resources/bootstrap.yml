server:
  port: 8082

spring:
  application:
    name: FootballPlayers
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profileSeparator: '::'
        format: YAML
   